services:
  - type: web
    name: gestion-entreprises-cnss
    runtime: node
    plan: starter
    region: frankfurt
    buildCommand: npm ci --include=dev && npm run build && cd build && npm ci --omit=dev
    preDeployCommand: cd build && node ace migration:run --force
    startCommand: cd build && npm run start
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: LOG_LEVEL
        value: info
      - key: SESSION_DRIVER
        value: cookie
      - key: APP_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: gestion-entreprises-cnss-db
          property: connectionString

databases:
  - name: gestion-entreprises-cnss-db
    region: frankfurt
    plan: basic-256mb
    diskSizeGB: 1
    databaseName: gestion_entreprises_cnss
    postgresMajorVersion: "18"
